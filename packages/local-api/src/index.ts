export const serve = (port: number, dir: string, filename: string) => {
	console.log("Serving traffic on port: ", port);
	console.log("Saving/fetching files from dir: ", filename);
	console.log("That file is in directory: ", dir);
};
